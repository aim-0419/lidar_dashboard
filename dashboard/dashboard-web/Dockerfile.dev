FROM node:22-bookworm-slim

WORKDIR /app

# 의존성 파일을 먼저 복사하여 소스 변경 시 npm ci 레이어를 재사용한다.
COPY package*.json ./
RUN npm ci

COPY . .

EXPOSE 5173

# 외부 브라우저가 컨테이너의 Vite 개발 서버에 접근할 수 있도록 전체 인터페이스에 바인딩한다.
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
