# Docker

팀 공통 개발 실행 기준은 Docker Desktop + `docker compose up` 이다.

- 실제 실행값은 `.env`를 참조한다.
- `.env`의 비밀값은 커밋, 문서, 로그에 노출하지 않는다.
- 컨테이너 간 통신은 가능한 서비스명을 사용한다.
- 포트, 볼륨, 네트워크, 서비스명 변경은 승인 후 진행한다.
- 의존성 설치 방식 변경은 팀 실행 흐름에 영향이 있으므로 보고한다.
- 팀원이 복사해 실행할 명령은 짧고 정확하게 적는다.
