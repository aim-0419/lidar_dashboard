# Frontend

프론트엔드는 기존 사용자 흐름을 깨지 않는 것이 우선이다.

- 기존 라우팅, 상태 관리, API 호출 위치를 따른다.
- 요청하지 않은 화면, 컴포넌트, 디자인 변경은 추가하지 않는다.
- 로딩, 에러, 빈 상태는 기존 패턴이 있으면 재사용한다.
- 하드코딩된 API 주소를 추가하지 않는다.
- 텍스트가 버튼, 카드, 테이블에서 넘치지 않게 한다.
- 가능하면 데스크톱과 모바일 폭을 함께 확인한다.
